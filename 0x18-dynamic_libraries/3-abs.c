#include "main.h"
/**
 *  *  _abs - provide tje absolute value of input
 *   * @n: the value from the user
 *    * Return: absolute value of the input
 *    **/
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
